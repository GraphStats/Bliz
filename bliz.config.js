// bliz.config.js
module.exports = {
  // Root directory of the project (default: current working directory)
  root: process.cwd(),
  // Development server port
  port: 3000,
  // Plugins array (future extension point)
  plugins: []
};
